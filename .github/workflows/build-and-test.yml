#
# This source file is part of the Anonymous PocketTSLM project
#
# SPDX-FileCopyrightText: 2025 The Authors
#
# SPDX-License-Identifier: MIT
#

name: Build and Test

on:
  pull_request:
  workflow_dispatch:
  workflow_call:

concurrency:
  group: Build-and-Test-${{ github.ref }}
  cancel-in-progress: true

jobs:
  buildandtest:
    name: Build and Test
    uses: Anonymous/.github/.github/workflows/xcodebuild-or-fastlane.yml@v2
    permissions:
      contents: read
    with:
      artifactname: PocketTSLMStudy.xcresult
      runsonlabels: '["macOS", "self-hosted"]'
      fastlanelane: test
      customcommand: "fastlane test"
  uploadcoveragereport:
    name: Upload Coverage Report
    needs: buildandtest
    uses: Anonymous/.github/.github/workflows/create-and-upload-coverage-report.yml@v2
    permissions:
      contents: read
    with:
      coveragereports: PocketTSLMStudy.xcresult
    secrets:
      token: ${{ secrets.CODECOV_TOKEN }}
